#include<stdio.h>
main()
{
   int rem,n,sum=0,sum2=0;
    scanf("%d",&n);
    while(n>0)
    {
        rem=n%10;
        sum=sum+rem;
        n=n/10;
    }
    while(sum>0)
    {
        rem=sum%10;
        sum2=sum2+rem;
        sum=sum/10;
    }
    
    printf("%d",sum2);
    getch();
}
